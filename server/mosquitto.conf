# Mosquitto Configuration for living.ines.systems
# I allow anonymous connections since this is a prototype.
# For production, I'd add authentication with a password file.

# Listen on all interfaces, port 1883 (standard MQTT)
listener 1883

# Allow connections without username/password
allow_anonymous true
